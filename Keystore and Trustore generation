keytool -genkey -alias serverKey -keyalg RSA -keystore serverkeystore.jks -keysize 2048

openssl req -new -x509 -keyout ca-key -out ca-cert

keytool -keystore serverkeystore.jks -alias serverKey -certreq -file cert-file

keytool -keystore clienttruststore.jks -alias clientTrust -import -file ca-cert



SCRIPT DE TESTE PARA TESTAR FALHAS DE REPLICAS E VARIAS OPERAÇÕES COMO SE FOSSE BENCHMARK
throughput sao as x operações por segundo
comparar sem falhas, com falhas, etc

gerar par de chaves + servidor tem que ter uma keystore
tem que gerar o certificado do servidor a partir da keystore
o certificado do servidor é importado para a truststore do cliente

tudo sao transações, mesmo as de leitura, e tudo tem que ser guardado


keytool -genkey -alias serverkey -keyalg RSA -keypass password -keystore serverkeystore.jks -storepass password

keytool -export -alias serverkey -file serverkey.cer -keystore serverkeystore.jks -storepass password

keytool -import -v -trustcacerts -alias clientTrust -keypass password -file serverkey.cer -keystore clientcacerts.jks -storepass password
