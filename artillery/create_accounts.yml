config:
  target: "https://staging.example.com"
  phases:
    - duration: 15
      arrivalCount: 1 
scenario:
  - name: Create account
    flow:
    - log: "Creating accounts"
      - post:
          url: "/account"
