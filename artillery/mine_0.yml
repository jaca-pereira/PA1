config:
  target: "http://127.0.0.1:8080"
  phases:
    - duration: 60
      arrivalCount: 1
  payload:
    path: "./mineiros.csv"
    fields:
      - "miners"
scenarios:
  - name: Mine
    flow:
      - post:
          url: "/mine"
          json: "{{miners}}"

      