config:
  target: "http://172.19.0.2:8080"
  phases:
    - duration: 300
      arrivalCount: 5
  payload:
    path: "./mineiros.csv"
    fields:
      - "miners"
scenarios:
  - name: Mine
    flow:
      - post:
          url: "/mine"
          json: "{{miners}}"

      